// @ts-nocheck
/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult, UseMutationOptions, UseMutationResult } from "@tanstack/react-query";
import { queryOptions, useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { ChannelOut, SetIntervalRequest } from "../models/ChannelOut.ts";
import { listChannels, getChannel, toggleChannel, setChannelInterval } from "../client/channels.ts";

/* ============ useListChannels ============ */

export const listChannelsQueryKey = () => [{ url: '/channels' }] as const;

export function listChannelsQueryOptions(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryKey = listChannelsQueryKey();
  return queryOptions<ChannelOut[], ResponseErrorConfig<Error>, ChannelOut[], typeof queryKey>({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return listChannels(config);
    },
  });
}

export function useListChannels<
  TData = ChannelOut[],
  TQueryData = ChannelOut[],
  TQueryKey extends QueryKey = ReturnType<typeof listChannelsQueryKey>,
>(
  options: {
    query?: Partial<QueryObserverOptions<ChannelOut[], ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOpts } = queryConfig;
  const queryKey = queryOpts?.queryKey ?? listChannelsQueryKey();
  const query = useQuery({
    ...listChannelsQueryOptions(config),
    queryKey,
    ...queryOpts,
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };
  query.queryKey = queryKey as TQueryKey;
  return query;
}

/* ============ useGetChannel ============ */

export const getChannelQueryKey = ({ channel_id }: { channel_id: number }) =>
  [{ url: '/channels/:channel_id', params: { channel_id } }] as const;

export function getChannelQueryOptions(
  { channel_id }: { channel_id: number },
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getChannelQueryKey({ channel_id });
  return queryOptions<ChannelOut, ResponseErrorConfig<Error>, ChannelOut, typeof queryKey>({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getChannel({ channel_id }, config);
    },
  });
}

export function useGetChannel<
  TData = ChannelOut,
  TQueryData = ChannelOut,
  TQueryKey extends QueryKey = ReturnType<typeof getChannelQueryKey>,
>(
  { channel_id }: { channel_id: number },
  options: {
    query?: Partial<QueryObserverOptions<ChannelOut, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOpts } = queryConfig;
  const queryKey = queryOpts?.queryKey ?? getChannelQueryKey({ channel_id });
  const query = useQuery({
    ...getChannelQueryOptions({ channel_id }, config),
    queryKey,
    ...queryOpts,
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };
  query.queryKey = queryKey as TQueryKey;
  return query;
}

/* ============ useToggleChannel (mutation) ============ */

export function useToggleChannel(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryClient = useQueryClient();
  return useMutation<ChannelOut, ResponseErrorConfig<Error>, { channel_id: number }>({
    mutationFn: async ({ channel_id }) => toggleChannel({ channel_id }, config),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [{ url: '/channels' }] });
    },
  });
}

/* ============ useSetChannelInterval (mutation) ============ */

export function useSetChannelInterval(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const queryClient = useQueryClient();
  return useMutation<ChannelOut, ResponseErrorConfig<Error>, { channel_id: number; data: SetIntervalRequest }>({
    mutationFn: async ({ channel_id, data }) => setChannelInterval({ channel_id }, data, config),
    onSuccess: (_, vars) => {
      queryClient.invalidateQueries({ queryKey: [{ url: '/channels' }] });
      queryClient.invalidateQueries({ queryKey: [{ url: '/channels/:channel_id', params: { channel_id: vars.channel_id } }] });
    },
  });
}
