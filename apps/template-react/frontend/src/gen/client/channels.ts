// @ts-nocheck
/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@kubb/plugin-client/clients/axios";
import type { RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { ChannelOut, SetIntervalRequest } from "../models/ChannelOut.ts";

/* ============ GET /channels ============ */

function getListChannelsUrl() {
  return { method: 'GET' as const, url: `/channels` as const };
}

export async function listChannels(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config;
  const res = await request<ChannelOut[], ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getListChannelsUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}

/* ============ GET /channels/{channel_id} ============ */

export type GetChannelPathParams = { channel_id: number };

function getChannelUrl({ channel_id }: GetChannelPathParams) {
  return { method: 'GET' as const, url: `/channels/${channel_id}` as const };
}

export async function getChannel(
  { channel_id }: GetChannelPathParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;
  const res = await request<ChannelOut, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getChannelUrl({ channel_id }).url.toString(),
    ...requestConfig,
  });
  return res.data;
}

/* ============ POST /channels/{channel_id}/toggle ============ */

export type ToggleChannelPathParams = { channel_id: number };

function getToggleChannelUrl({ channel_id }: ToggleChannelPathParams) {
  return { method: 'POST' as const, url: `/channels/${channel_id}/toggle` as const };
}

export async function toggleChannel(
  { channel_id }: ToggleChannelPathParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;
  const res = await request<ChannelOut, ResponseErrorConfig<Error>, unknown>({
    method: "POST",
    url: getToggleChannelUrl({ channel_id }).url.toString(),
    ...requestConfig,
  });
  return res.data;
}

/* ============ POST /channels/{channel_id}/interval ============ */

export type SetChannelIntervalPathParams = { channel_id: number };

function getSetChannelIntervalUrl({ channel_id }: SetChannelIntervalPathParams) {
  return { method: 'POST' as const, url: `/channels/${channel_id}/interval` as const };
}

export async function setChannelInterval(
  { channel_id }: SetChannelIntervalPathParams,
  data: SetIntervalRequest,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;
  const res = await request<ChannelOut, ResponseErrorConfig<Error>, SetIntervalRequest>({
    method: "POST",
    url: getSetChannelIntervalUrl({ channel_id }).url.toString(),
    data,
    ...requestConfig,
  });
  return res.data;
}
